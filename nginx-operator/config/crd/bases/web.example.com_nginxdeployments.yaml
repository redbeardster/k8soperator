---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: nginxdeployments.web.example.com
spec:
  group: web.example.com
  names:
    kind: NginxDeployment
    listKind: NginxDeploymentList
    plural: nginxdeployments
    singular: nginxdeployment
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: NginxDeployment is the Schema for the nginxdeployments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NginxDeploymentSpec defines the desired state of NginxDeployment
            properties:
              image:
                description: Docker image for nginx
                type: string
              port:
                description: Port for nginx container
                format: int32
                type: integer
              replicas:
                description: Number of nginx replicas
                format: int32
                type: integer
            required:
            - replicas
            type: object
          status:
            description: NginxDeploymentStatus defines the observed state of NginxDeployment
            properties:
              availableReplicas:
                description: Number of available replicas
                format: int32
                type: integer
              status:
                description: Status message
                type: string
            required:
            - availableReplicas
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
